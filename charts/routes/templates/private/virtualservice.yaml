{{- if .Values.public.enabled }}
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ .Release.Name }}-private
  labels: {{- include "library.chart.labels" . | nindent 4 }}
spec:
  gateways:
    - {{ .Release.Name }}-private
  hosts:
    {{- include "domains.private" . | nindent 4 }}
  http:
    - name: default
      {{- if len .Values.public.privatePaths }}
      match:
        {{- range ( concat ( list "/health" "/swagger" "/metrics" ) .Values.public.privatePaths | uniq ) }}
        - uri:
            prefix: {{ .  }}
        {{- end }}
      {{- end }}
      route:
        - destination:
            host: {{ include "upstream" . }}
            port:
              number: {{ .Values.global.upstream.port }}
      retries:
        attempts: 3
        perTryTimeout: 0.1s
        retryOn: gateway-error,connect-failure,refused-stream
{{- end }}
